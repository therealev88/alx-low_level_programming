#include "main.h"
#include <stdio.h>
/**
 * main- Entry point
 * Description: a function that checks if an integer is + or -
 * Return: Point of no return (void)
 */
int main(void)
{
	int i;

	i = 0;
	positive_or_negative(i);

	return (0);
}
